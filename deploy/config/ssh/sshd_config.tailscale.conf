# Append these lines to /etc/ssh/sshd_config after Tailscale SSH is confirmed.

PermitRootLogin no
PasswordAuthentication no
PubkeyAuthentication yes

# Replace with your real Tailscale IPv4 address.
ListenAddress 100.x.x.x
